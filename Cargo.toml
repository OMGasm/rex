[package]
name = "rex"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.30", features = ["derive"] }
crossterm = { version = "0.28.1", features = ["bracketed-paste", "event-stream"], default-features = false }
